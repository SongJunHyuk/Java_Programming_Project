/*
package RoomPackage;

public class TimeTable {
	int[][] timeTable;
	timeTable = new int[9][7];
	
	int timeRequest;
	boolean possible;
	
	public TimeTable() {}
	
	public TimeTable(int[][] timeTable, int timeRequest, boolean possible) {
		this.timeTable = timeTable;
		this.timeRequest = timeRequest;
		this.possible = possible;
	}
	
	public String TimeTable(boolean possible, int timeRequest ) {
		if( possible == false) {
			return "Request is denied";
		}
		else {
			for(int i = 0; i<=timeRequest; i++) {
				
			}
			return "Request is accepted";
		}
	}
	
	public void printTimeTable() {
		for(int i = 0; i <= 8; i++) {
			for(int j = 0; j <= 6; j++) {
				  print TimeTable
			}
		}
	}
}
*/
